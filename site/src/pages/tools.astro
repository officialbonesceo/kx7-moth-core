---
import Base from '../layouts/Base.astro';
import BannerAd from '../components/BannerAd.astro';

const origin = Astro.url.origin;
const SMARTLINK = 'https://omg10.com/4/11821454';
const jsonLd = [
  {
    '@context': 'https://schema.org',
    '@type': 'WebApplication',
    name: 'LaneCash Money Tools',
    applicationCategory: 'FinanceApplication',
    operatingSystem: 'Any',
    url: `${origin}/tools`,
    description:
      'Educational tools: any-to-any currency converter (USD, NGN, KWD, EUR…), crypto estimates, fees, news, tx safety. Not financial advice.',
    offers: { '@type': 'Offer', price: '0', priceCurrency: 'NGN' },
  },
];
---
<Base
  title="Money Tools — Currency, Crypto & Fees | LaneCash"
  description="Educational tools: global FX converter (USD, NGN, KWD…), crypto estimates, fee impact, news, and tx safety. Not financial advice."
  jsonLd={jsonLd}
>
  <div class="disclaimer-banner">
    <strong>Disclaimer:</strong> Estimates & public data only — not financial advice.
    <a href="/disclaimer">Full disclaimer</a>
  </div>

  <section class="hero">
    <h1>Money tools</h1>
    <p>FX, fees, crypto estimates, news, and safety checks — educational only.</p>
  </section>

  <BannerAd size="320x50" />

  <a class="calc-card" href="/tools/usd-ngn" style="display:block;max-width:720px;margin:0 auto 20px;text-decoration:none;color:inherit">
    <h3 style="margin:0 0 6px">Currency converter (any → any)</h3>
    <p class="muted" style="margin:0;font-size:.85rem;line-height:1.45">
      USD, NGN, KWD, EUR, GBP, AED and more — mid-market estimates, not bank or CBN quotes.
    </p>
  </a>

  <div class="calc-card" style="max-width:720px;margin:0 auto 20px">
    <h3 style="margin:0 0 6px">Partner offers</h3>
    <p class="muted" style="margin:0 0 12px;font-size:.85rem;line-height:1.45">
      Optional third-party offers. LaneCash does not control external pages — review carefully. Not financial advice.
    </p>
    <a class="save-btn calc-primary" href={SMARTLINK} target="_blank" rel="noopener noreferrer sponsored">View offers</a>
  </div>

  <div style="max-width:720px;margin:0 auto 16px;max-height:280px;overflow:hidden">
    <BannerAd size="300x250" />
  </div>

  <div class="calc-tabs" role="tablist">
    <button type="button" class="chip active" data-tab="crypto">Crypto</button>
    <button type="button" class="chip" data-tab="fee">Fees</button>
    <button type="button" class="chip" data-tab="hustle">Hustle</button>
    <button type="button" class="chip" data-tab="news">News</button>
    <button type="button" class="chip" data-tab="safety">Tx safety</button>
  </div>

  <div class="calc-panel" id="panel-crypto">
    <div class="calc-card">
      <h3>Crypto price estimate (USD & ₦)</h3>
      <p class="muted" style="margin:0 0 10px;font-size:.82rem">
        Public market data only — not a trading signal or financial advice.
      </p>
      <label for="coin">Coin</label>
      <select id="coin">
        <option value="tether">USDT</option>
        <option value="bitcoin">Bitcoin (BTC)</option>
        <option value="ethereum">Ethereum (ETH)</option>
      </select>
      <label for="coinQty">Quantity</label>
      <input id="coinQty" type="number" inputmode="decimal" value="100" min="0" step="any" />
      <button class="save-btn calc-primary" id="coinBtn" type="button">Get estimate</button>
      <div class="calc-result" id="coinOut">—</div>
      <p class="muted" id="coinNote" style="margin-top:8px;font-size:.75rem"></p>
    </div>
  </div>

  <div class="calc-panel" id="panel-fee" hidden>
    <div class="calc-card">
      <h3>Fee impact</h3>
      <p class="muted" style="margin:0 0 10px;font-size:.82rem">See how a % fee changes what you keep — educational math only.</p>
      <label for="feeAmt">Amount (₦)</label>
      <input id="feeAmt" type="number" inputmode="decimal" value="50000" min="0" />
      <label for="feePct">Fee %</label>
      <input id="feePct" type="number" inputmode="decimal" value="1.5" min="0" step="0.1" />
      <div class="fee-presets">
        <button type="button" class="chip" data-fee="0.5">0.5%</button>
        <button type="button" class="chip" data-fee="1">1%</button>
        <button type="button" class="chip" data-fee="1.5">1.5%</button>
        <button type="button" class="chip" data-fee="2">2%</button>
      </div>
      <button class="save-btn calc-primary" id="feeBtn" type="button">Calculate fee</button>
      <div class="calc-result" id="feeOut">—</div>
    </div>
  </div>

  <div class="calc-panel" id="panel-hustle" hidden>
    <div class="calc-card">
      <h3>Weekly practice planner</h3>
      <p class="muted" style="margin:0 0 10px;font-size:.82rem">Plan practice volume — not a promise of earnings.</p>
      <label for="target">Weekly target (₦)</label>
      <input id="target" type="number" value="20000" min="0" />
      <label for="days">Work days / week</label>
      <input id="days" type="number" value="5" min="1" max="7" />
      <label for="perJob">Amount per task (₦)</label>
      <input id="perJob" type="number" value="2000" min="1" />
      <button class="save-btn calc-primary" id="hustleBtn" type="button">Plan my week</button>
      <div class="calc-result" id="hustleOut">—</div>
    </div>
  </div>

  <div class="calc-panel" id="panel-news" hidden>
    <div class="calc-card">
      <h3>Crypto headlines</h3>
      <p class="muted" style="margin:0 0 10px;font-size:.82rem">Public headlines only — not trade recommendations.</p>
      <button class="save-btn calc-primary" id="newsBtn" type="button">Refresh news</button>
      <div id="newsList" style="margin-top:12px"></div>
    </div>
  </div>

  <div class="calc-panel" id="panel-safety" hidden>
    <div class="calc-card">
      <h3>Tx / address safety helper</h3>
      <p class="muted" style="margin:0 0 10px;font-size:.85rem;line-height:1.45">
        Paste a transaction hash or wallet address to open an official explorer. This does not recover funds or prove someone is safe.
      </p>
      <label for="chain">Network</label>
      <select id="chain">
        <option value="eth">Ethereum</option>
        <option value="bsc">BNB Smart Chain</option>
        <option value="polygon">Polygon</option>
        <option value="tron">Tron (USDT TRC-20)</option>
      </select>
      <label for="txInput">Tx hash or address</label>
      <input id="txInput" type="text" placeholder="0x… or T…" autocomplete="off" />
      <button class="save-btn calc-primary" id="txBtn" type="button">Open in explorer</button>
      <div class="calc-result" id="txOut" style="font-weight:600">Enter a hash or address</div>
    </div>
  </div>

  <script is:inline>
    (function () {
      var origin = window.location.origin;

      document.querySelectorAll('.calc-tabs .chip').forEach(function (btn) {
        btn.addEventListener('click', function () {
          document.querySelectorAll('.calc-tabs .chip').forEach(function (b) {
            b.classList.remove('active');
          });
          btn.classList.add('active');
          var tab = btn.getAttribute('data-tab');
          ['crypto', 'fee', 'hustle', 'news', 'safety'].forEach(function (id) {
            var p = document.getElementById('panel-' + id);
            if (p) p.hidden = id !== tab;
          });
          if (tab === 'news') loadNews();
        });
      });

      async function coin() {
        var id = document.getElementById('coin').value;
        var qty = +document.getElementById('coinQty').value || 0;
        var out = document.getElementById('coinOut');
        var note = document.getElementById('coinNote');
        out.textContent = 'Loading…';
        note.textContent = '';
        try {
          var r = await fetch(
            origin +
              '/api/crypto/price?id=' +
              encodeURIComponent(id) +
              '&qty=' +
              encodeURIComponent(qty)
          );
          var j = await r.json();
          out.textContent =
            qty +
            ' ≈ $' +
            Number(j.total_usd).toLocaleString(undefined, { maximumFractionDigits: 2 }) +
            ' · ₦' +
            Number(j.total_ngn).toLocaleString(undefined, { maximumFractionDigits: 0 });
          note.textContent =
            'Estimate · source: ' + (j.source || 'api') + (j.cached ? ' (cached)' : '') + ' · not financial advice';
        } catch (e) {
          out.textContent = 'Could not load estimate';
          note.textContent = 'Try again in a moment';
        }
      }

      function fee() {
        var amt = +document.getElementById('feeAmt').value || 0;
        var pct = +document.getElementById('feePct').value || 0;
        var f = amt * (pct / 100);
        document.getElementById('feeOut').textContent =
          'Fee ₦' +
          f.toLocaleString(undefined, { maximumFractionDigits: 2 }) +
          ' · Left ₦' +
          (amt - f).toLocaleString(undefined, { maximumFractionDigits: 2 });
      }

      function hustle() {
        var target = +document.getElementById('target').value || 0;
        var days = Math.max(1, +document.getElementById('days').value || 1);
        var per = Math.max(1, +document.getElementById('perJob').value || 1);
        var jobs = Math.ceil(target / per);
        var jobsPerDay = Math.ceil(jobs / days);
        document.getElementById('hustleOut').textContent =
          '~' +
          jobs +
          ' tasks/week · ~' +
          jobsPerDay +
          '/day · target ₦' +
          Math.round(target / days).toLocaleString() +
          '/day (practice math only)';
      }

      async function loadNews() {
        var box = document.getElementById('newsList');
        box.innerHTML = '<p class="muted">Loading…</p>';
        try {
          var r = await fetch(origin + '/api/crypto/news');
          var j = await r.json();
          var items = j.items || [];
          if (!items.length) {
            box.innerHTML = '<p class="muted">No headlines right now.</p>';
            return;
          }
          box.innerHTML = items
            .map(function (it) {
              var href = it.url || '#';
              if (href.charAt(0) === '/') href = origin + href;
              return (
                '<a class="card" style="grid-template-columns:1fr;margin-bottom:8px" href="' +
                href +
                '" target="_blank" rel="noopener noreferrer">' +
                '<div class="card-main"><span class="pill">' +
                (it.source || 'News') +
                '</span>' +
                '<h3 style="font-size:.9rem">' +
                (it.title || '') +
                '</h3></div></a>'
              );
            })
            .join('');
        } catch (e) {
          box.innerHTML = '<p class="muted">News temporarily unavailable.</p>';
        }
      }

      function openTx() {
        var chain = document.getElementById('chain').value;
        var q = (document.getElementById('txInput').value || '').trim();
        var out = document.getElementById('txOut');
        if (!q || q.length < 6) {
          out.textContent = 'Paste a full tx hash or address';
          return;
        }
        var isTx = /^0x([a-fA-F0-9]{64})$/.test(q) || /^[a-fA-F0-9]{64}$/.test(q);
        var bases = {
          eth: { tx: 'https://etherscan.io/tx/', addr: 'https://etherscan.io/address/' },
          bsc: { tx: 'https://bscscan.com/tx/', addr: 'https://bscscan.com/address/' },
          polygon: { tx: 'https://polygonscan.com/tx/', addr: 'https://polygonscan.com/address/' },
          tron: { tx: 'https://tronscan.org/#/transaction/', addr: 'https://tronscan.org/#/address/' },
        };
        var b = bases[chain] || bases.eth;
        var url;
        if (chain === 'tron') url = (isTx ? b.tx : b.addr) + q;
        else if (isTx) url = b.tx + (q.indexOf('0x') === 0 ? q : '0x' + q);
        else url = b.addr + (q.indexOf('0x') === 0 ? q : '0x' + q);
        out.textContent = 'Opening official explorer…';
        window.open(url, '_blank', 'noopener,noreferrer');
      }

      document.getElementById('coinBtn').onclick = coin;
      document.getElementById('feeBtn').onclick = fee;
      document.getElementById('hustleBtn').onclick = hustle;
      document.getElementById('newsBtn').onclick = loadNews;
      document.getElementById('txBtn').onclick = openTx;
      document.querySelectorAll('.fee-presets .chip').forEach(function (b) {
        b.addEventListener('click', function () {
          document.getElementById('feePct').value = b.getAttribute('data-fee');
          fee();
        });
      });

      coin();
      fee();
      hustle();
    })();
  </script>
</Base>
